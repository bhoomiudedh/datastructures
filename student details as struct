#include <stdio.h>

int main() {
    struct Stu{
        char name[20];
        char usn[10];
        int phone;
    }s1;
    printf("enter details:");
    scanf("%s%s%d",s1.name,s1.usn,s1.phone);
    printf("\n%s,%s,%d\n",s1.name,s1.usn,s1.phone);

}
